<?php
/*
require_once("src/core/init.php");

$productController = new ProductController();

if (empty($id)) {
    $id = escape(Input::get("product_id"));
    // TODO: Add product to the cart
} else {
    // TODO: Check if id matches a product in DB and add to Cart if true
}
*/